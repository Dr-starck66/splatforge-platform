# ══════════════════════════════════════════════════════════
# SPLAT·FORGE — Python Dependencies
# ══════════════════════════════════════════════════════════

# API
fastapi==0.111.0
uvicorn[standard]==0.29.0
python-multipart==0.0.9
aiofiles==23.2.1

# Vision
opencv-python-headless==4.9.0.80
Pillow==10.3.0
numpy==1.26.4
scipy==1.13.0

# SfM
pycolmap==0.6.1

# Depth estimation (MiDaS via torch.hub — no explicit dep needed)
# gsplat installed separately in Dockerfile (CUDA compile)

# Utilities
pydantic==2.7.1
python-dotenv==1.0.1

# Optional: queue system
# celery[redis]==5.3.6
# flower==2.0.1
